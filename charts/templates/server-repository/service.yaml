{{- if .Values.serverRepository.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "sourcify.fullname" . }}-serverRepository
  labels:
    {{- include "sourcify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.serverRepository.service.type }}
  ports:
    - port: {{ .Values.serverRepository.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "sourcify.serverRepository.selectorLabels" . | nindent 4 }}
{{- end }}    
