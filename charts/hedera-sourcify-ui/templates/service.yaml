apiVersion: v1
kind: Service
metadata:
  name: {{ include "hedera-sourcify-ui.fullname" . }}
  labels:
    {{- include "hedera-sourcify-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hedera-sourcify-ui.selectorLabels" . | nindent 4 }}
