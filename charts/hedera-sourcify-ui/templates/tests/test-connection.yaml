apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hedera-sourcify-ui.fullname" . }}-test-connection"
  labels:
    {{- include "hedera-sourcify-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hedera-sourcify-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
