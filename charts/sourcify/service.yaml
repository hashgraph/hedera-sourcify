apiVersion: v1
kind: Service
metadata:
  name: {{ include "sourcify.fullname" . }}
  labels:
    {{- include "sourcify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sourcify.selectorLabels" . | nindent 4 }}
