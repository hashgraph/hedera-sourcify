apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hedera-sourcify.fullname" . }}-test-connection"
  labels:
    {{- include "hedera-sourcify.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hedera-sourcify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
