apiVersion: v1
kind: Service
metadata:
  name: {{ include "hedera-sourcify-server.fullname" . }}
  labels:
    {{- include "hedera-sourcify-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hedera-sourcify-server.selectorLabels" . | nindent 4 }}
