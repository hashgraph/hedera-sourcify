apiVersion: v1
kind: Service
metadata:
  name: {{ include "hedera-sourcify-repository.fullname" . }}
  labels:
    {{- include "hedera-sourcify-repository.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hedera-sourcify-repository.selectorLabels" . | nindent 4 }}
