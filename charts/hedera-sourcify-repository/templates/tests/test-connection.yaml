apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hedera-sourcify-repository.fullname" . }}-test-connection"
  labels:
    {{- include "hedera-sourcify-repository.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hedera-sourcify-repository.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
